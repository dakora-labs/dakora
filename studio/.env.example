### Studio environment example
# When VITE_AUTH_REQUIRED is set to 'false' the frontend will skip Clerk and run unauthenticated.
# Default is true (or unset) which enables Clerk and requires VITE_CLERK_PUBLISHABLE_KEY.

# ============================================================================
# AUTHENTICATION
# ============================================================================
# Set to 'false' to disable auth in UI for local development against unauthenticated backend APIs
# For production with invite-only access, set to 'true'
VITE_AUTH_REQUIRED=true

# Clerk publishable key (only needed when VITE_AUTH_REQUIRED is true)
# Get this from: Clerk Dashboard > API Keys > Publishable Key
# Format: pk_test_... (test) or pk_live_... (production)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_KEY_HERE

# ============================================================================
# API ENDPOINT
# ============================================================================
# Backend API URL (defaults to http://localhost:8000 if not set)
# Production: https://api.dakora.io
# VITE_API_URL=http://localhost:8000

# ============================================================================
# ERROR TRACKING (Optional)
# ============================================================================
# Sentry DSN for error tracking (only used when VITE_AUTH_REQUIRED is true)
# VITE_SENTRY_DSN=https://YOUR_SENTRY_DSN@sentry.io/PROJECT_ID
