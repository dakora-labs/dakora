name: Release CLI (dakora)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 2.0.0)'
        required: true
        type: string

jobs:
  release-cli:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
      id-token: write

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"

    - name: Set up Python 3.11
      run: uv python install 3.11

    - name: Update CLI version
      run: |
        cd cli
        uv run python -c "
        import tomllib
        import tomli_w

        with open('pyproject.toml', 'rb') as f:
            data = tomllib.load(f)

        data['project']['version'] = '${{ github.event.inputs.version }}'

        with open('pyproject.toml', 'wb') as f:
            tomli_w.dump(data, f)
        "

    - name: Install dependencies
      run: |
        cd cli
        uv sync

    - name: Run CLI tests
      run: |
        cd cli
        uv run pytest tests/ -v || echo "No CLI tests yet"

    - name: Build CLI package
      run: |
        cd cli
        uv build

    - name: Check package
      run: |
        cd cli
        uv run twine check dist/*

    - name: Smoke test CLI
      run: |
        python -m venv /tmp/test-cli
        /tmp/test-cli/bin/pip install cli/dist/*.whl
        /tmp/test-cli/bin/dakora --help
        echo "✅ CLI smoke test passed"

    - name: Publish to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
      run: |
        cd cli
        uv run twine upload dist/*

    - name: Create git tag
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add cli/pyproject.toml
        git commit -m "chore: bump CLI version to ${{ github.event.inputs.version }}"
        git tag -a "cli-v${{ github.event.inputs.version }}" -m "CLI Release v${{ github.event.inputs.version }}"
        git push origin main
        git push origin "cli-v${{ github.event.inputs.version }}"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Create GitHub Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: "cli-v${{ github.event.inputs.version }}"
        release_name: "CLI v${{ github.event.inputs.version }}"
        body: |
          ## Dakora CLI v${{ github.event.inputs.version }}

          Install with:
          ```bash
          pip install dakora==${{ github.event.inputs.version }}
          ```

          ### Usage
          ```bash
          dakora init
          dakora start
          ```
        draft: false
        prerelease: false